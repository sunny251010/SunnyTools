---
import { defaultLocale, type Locale } from "../../../i18n/config";
import LineToolsTool from "../line-tools/LineToolsTool.astro";

interface Props {
  locale?: Locale;
}

const { locale = defaultLocale } = Astro.props;
---

<LineToolsTool locale={locale} defaultMode="remove-duplicates" />
