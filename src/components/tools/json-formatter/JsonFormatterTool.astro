---
import { defaultLocale, type Locale } from "../../../i18n/config";
import JsonToolsTool from "../json-tools/JsonToolsTool.astro";

interface Props {
  locale?: Locale;
}

const { locale = defaultLocale } = Astro.props;
---

<JsonToolsTool locale={locale} defaultMode="format" />
