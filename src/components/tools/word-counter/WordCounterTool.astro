---
import { defaultLocale, type Locale } from "../../../i18n/config";
import TextCounterTool from "../text-counter/TextCounterTool.astro";

interface Props {
  locale?: Locale;
}

const { locale = defaultLocale } = Astro.props;
---

<TextCounterTool locale={locale} defaultMode="all" />
